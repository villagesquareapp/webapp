import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Messages | Village Square Dashboard",
};

const MessagesPage = () => {
  return <div>Enter</div>;
};

export default MessagesPage;
